{
  "name": "3innova/contact",
  "description": "Formulario de contacto de 3innova.io — /api/contact.php (PHP 8.x, sin frameworks). PHPMailer vendorizado en el repo (no se ejecuta Composer en el servidor).",
  "type": "project",
  "license": "proprietary",
  "require": {
    "php": ">=8.1",
    "phpmailer/phpmailer": "^6.9"
  },
  "config": {
    "optimize-autoloader": true,
    "sort-packages": true
  }
}